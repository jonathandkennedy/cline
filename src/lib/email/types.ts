export type CaseReviewLead = {
	name: string;
	phone: string;
	email: string;
	vehicle: string;
	vehicleType: string;
	issue?: string;
	context?: string;
	estimate?: string;
	source?: string;
	pageUrl?: string;
	embedUrl?: string;
	submittedAt: string;
};
